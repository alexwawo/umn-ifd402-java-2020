package week08.finalmethoddemo;

public class Child extends Parent {
//    void say_hello() {}
    // can not override method say_hello() --
    // method with same name,
    // because say_hello declared as final at Parent
}
